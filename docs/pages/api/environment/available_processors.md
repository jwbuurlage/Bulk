# `bulk::environment::available_processors`

```cpp
int available_processors() const;
```

Returns the total number of processors available on the system.

## Return value

An `int` containing the number of available processors.
