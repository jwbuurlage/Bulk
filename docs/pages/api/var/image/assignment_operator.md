# `bulk::var::image::operator=`

```cpp
var<T>& operator=(const T& value);
```

Assign a value to a remote image

## Parameters

* `value` - the new value of the image

## Complexity and cost

* **Cost** - `sizeof(T) * g`
