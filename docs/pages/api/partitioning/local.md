# `bulk::partitioning::local`

```cpp
index_type<D> local(index_type<D> xs);
```

Convert a global index to a local one.

## Parameters

- `xs`: the global index

## Return value

- `index_type<D>`: the local index
