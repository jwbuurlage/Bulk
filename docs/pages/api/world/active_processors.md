# `bulk::world::active_processors`

```cpp
int active_processors() const;
```

Returns the total number of active processors in an SPMD section.

## Return value

An `int` containing the number of active processors.
